describe('rateLimitMiddleware', () => {
  it('increments request count on every request', () => {
    throw new Error('Not implemented yet');
  });

  it('returns 429 if limit is exceeded', () => {
    throw new Error('Not implemented yet');
  });

  it('calls next if request count is not greater than limit', () => {
    throw new Error('Not implemented yet');
  });
});
